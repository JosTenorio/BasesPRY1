CREATE TABLE [dbo].[AUTOMOVIL]
(
	[MODELO] VARCHAR(20) NOT NULL, 
    [ANO] NCHAR(4) NOT NULL, 
    CHECK (LEN(ANO) = 4),
    [DETALLE] VARCHAR(50) NOT NULL,
    [FABRICANTE] INT NOT NULL, 
    FOREIGN KEY ([FABRICANTE]) REFERENCES FAB_AUTOS (ID)
        ON DELETE NO ACTION
        ON UPDATE CASCADE,
    PRIMARY KEY (MODELO, ANO)
)
