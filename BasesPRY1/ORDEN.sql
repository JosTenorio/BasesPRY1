CREATE TABLE ORDEN
(
	[ID] INT NOT NULL PRIMARY KEY, 
    [MONTO_BASE] DECIMAL(18, 2) NOT NULL, 
    [MONTO_FINAL] DECIMAL(18, 2) NOT NULL, 
    [FECHA] DATE NOT NULL, 
    [CEDULA_CLIENTE] NCHAR(10) NOT NULL,
    FOREIGN KEY (CEDULA_CLIENTE) REFERENCES CLIENTE (CEDULA)
)
